type User @model {
  id: ID!
  email: String! @unique
  name: String!
}

type Game @model {
  id: ID!
  slug: String! @unique
  name: String! @unique
  linkUrl: String!
  logoImageUrl: String!
}

type GameScore @model {
  id: ID!
  game: Game!
  user: User!
  score: Int!
}
